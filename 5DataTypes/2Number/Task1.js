"use strict"
let a = "abcd";
console.log(0.1 + 0.2);

function sum(number1, number2) {
    let str1 = number1.toString();
    let str2 = number2.toString();

    for (let i = str.length-1; i >= 0; i-- )
    {
        let result = "";
        let memory = 0;
        let num = (+str1[i] + +str[i]).toString();
        if (a > 9)
        {
            memory = num[0];
            result += 0;
        }
    }
}